import LoginForm from './LoginForm'; // Import the component from the same folder

export default function SignupPage() {
    // This page's only job is to render the LoginForm component.
    return <LoginForm />;
}